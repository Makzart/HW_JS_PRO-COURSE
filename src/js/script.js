/* Example #1 */
const sum = (a, b) => a + b;

console.log(sum(4, 5));
/* Example #2 */
const strLength = (string) => string.length;

console.log(strLength('Hillel'));
/* Example #3 */
const incrementArray = (arr) => arr.map((num) => num + 1);
const numbers = [1, 2, 3, 4, 5, 6, 7];

console.log(incrementArray(numbers));
